[phases.setup]
nixPkgs = ["nodejs_20"]

[build]
builder = "nixpacks"
buildCommand = "cd api && npm install && npm run build"

[deploy]
startCommand = "cd api && npm run start:prod"
healthcheckPath = "/"
